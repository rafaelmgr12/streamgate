transports:
  - type: "http"
    addr: ":8080"

health_check:
  path: "/healthz"
  interval: "10s"
  timeout: "2s"

services:
  - name: "echo"
    path_prefix: "/api/echo/"
    backends:
      - "http://echo1:80"
      - "http://echo2:80"
